{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "fetch",
  "title": "Fetch",
  "description": "Download files from the web.",
  "icon": "fetch-icon.png",
  "author": "chrismessina",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Web",
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "download",
      "title": "Download",
      "subtitle": "Fetch",
      "description": "Download a single file from a URL.",
      "mode": "no-view",
      "arguments": [
        {
          "name": "url",
          "placeholder": "URL",
          "type": "text",
          "required": false
        }
      ]
    },
    {
      "name": "download-batch",
      "title": "Download Batch",
      "subtitle": "Fetch",
      "description": "Download multiple files from a list of URLs.",
      "mode": "view"
    },
    {
      "name": "download-history",
      "title": "Download History",
      "subtitle": "Fetch",
      "description": "View download history.",
      "mode": "view"
    },
    {
      "name": "download-sequential",
      "title": "Download Sequential",
      "subtitle": "Fetch",
      "description": "Download numbered sequences from URL patterns (e.g., image_{n}.jpg).",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "outputDirectory",
      "title": "Output Directory",
      "description": "Default directory to save downloaded files",
      "type": "directory",
      "default": "~/Downloads",
      "required": false
    },
    {
      "name": "followRedirects",
      "title": "Follow Redirects",
      "description": "Automatically follow HTTP redirects",
      "type": "checkbox",
      "default": true,
      "label": "Follow HTTP redirects",
      "required": false
    },
    {
      "name": "defaultTimeout",
      "title": "Timeout (seconds)",
      "description": "Download timeout in seconds",
      "type": "textfield",
      "default": "300",
      "required": false
    },
    {
      "name": "overwriteExisting",
      "title": "Overwrite Existing",
      "description": "Overwrite files if they already exist",
      "type": "checkbox",
      "default": false,
      "label": "Overwrite existing files",
      "required": false
    },
    {
      "name": "enableDebugLogging",
      "title": "Debug Logging",
      "description": "Enable verbose debug logging",
      "type": "checkbox",
      "default": false,
      "label": "Enable debug logging",
      "required": false
    },
    {
      "name": "maxParallelDownloads",
      "title": "Max Parallel Downloads",
      "description": "Maximum number of concurrent downloads for batch operations",
      "type": "textfield",
      "default": "3",
      "required": false
    }
  ],
  "dependencies": {
    "@chrismessina/raycast-logger": "^1.2.2",
    "@raycast/api": "^1.104.1",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
